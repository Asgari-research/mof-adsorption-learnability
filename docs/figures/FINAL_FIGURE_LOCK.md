# Publication figure lock

The eight PDFs under `figures/main/` and `figures/si/` are the authoritative publication assets for the finalized figure state dated 2026-09-21.

Figure regeneration writes only to `figure_generation/outputs/`. Regenerated files must not overwrite the authoritative PDFs automatically. Any replacement requires explicit review and an update to `FINAL_FIGURE_MANIFEST.csv`.
